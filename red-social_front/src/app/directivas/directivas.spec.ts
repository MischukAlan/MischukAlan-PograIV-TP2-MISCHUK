import { Directivas } from './directivas';

describe('Directivas', () => {
  it('should create an instance', () => {
    const directive = new Directivas();
    expect(directive).toBeTruthy();
  });
});
